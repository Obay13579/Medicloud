version: '3.8'

services:
  # --- Backend App ---
  backend:
    build: ./backend
    container_name: medicloudy-backend
    restart: always
    environment:
      # PENTING: Pastikan ini baca variable dari file .env
      DATABASE_URL: ${DATABASE_URL}
      PORT: 3000
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "3000:3000"
    networks:
      - medicloudy_network

  # --- Frontend App ---
  frontend:
    build: ./frontend
    container_name: medicloudy-frontend
    restart: always
    ports:
      - "5173:80"
    networks:
      - medicloudy_network

networks:
  medicloudy_network:
    name: medicloudy_network
    external: true
